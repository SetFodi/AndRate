{
  "name": "andrate",
  "version": "0.1.0",
  "private": true,
  "description": "AndRate - A modern cross-platform desktop app for browsing Anime, TV Shows, and Movies",
  "scripts": {
    "dev": "npm run tauri dev",
    "build": "npm run tauri build",
    "install-deps": "npm install --prefix ui",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0"
  }
}

